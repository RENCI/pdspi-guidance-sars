version: '3.4'
services:
  
  pdspi-guidance-sars-triage-test:
    build:
      context: test
    command: ["-s", "-vv", "-x"]
    depends_on:
      - pdspi-guidance-sars-triage
      - pdspi-test-pds-server

  pdspi-test-pds-server:
    build:
      context: test/pds-server-mock
